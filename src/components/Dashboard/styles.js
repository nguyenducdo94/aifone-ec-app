import { makeStyles } from '@material-ui/core/styles';

export default makeStyles((theme) => ({
   root: {

   },

   rootShift: {
      marginLeft: 240
   },

   container: {
      padding: 20,
      display: 'flex',
      alignContent: 'center',
      justifyContent: 'center'
   },

   content: {
      width: '50%',
      padding: 20
   }
}));